sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t hartw/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true hartw/react-test npm test

after_success:
  - docker build -t hartw/multi-client ./client
  - docker build -t hartw/multi-nginx ./nginx
  - docker build -t hartw/multi-server ./server
  - docker build -t hartw/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push hartw/multi-client
  - docker push hartw/multi-nginx
  - docker push hartw/multi-server
  - docker push hartw/multi-worker
